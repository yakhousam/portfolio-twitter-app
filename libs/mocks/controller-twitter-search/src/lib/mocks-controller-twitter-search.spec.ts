import { mocksControllerTwitterSearch } from './mocks-controller-twitter-search';

describe('mocksControllerTwitterSearch', () => {
  it('should work', () => {
    expect(mocksControllerTwitterSearch()).toEqual(
      'mocks-controller-twitter-search'
    );
  });
});
